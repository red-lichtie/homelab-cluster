---
apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: ceph
  namespace: rook-ceph
spec:
  parentRefs:
    - kind: Gateway
      name: main-gateway
      namespace: default
  hostnames:
    - ceph.home.svc
  rules:
    - backendRefs:
        - name: rook-ceph-mgr-dashboard
          port: 7000
...
